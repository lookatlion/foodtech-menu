# Shell scripts and Docker files must have LF line endings to run on Linux
gradlew          text eol=lf
Dockerfile       text eol=lf
docker-compose.yml text eol=lf
*.sh             text eol=lf

# All other text files — normalise to LF in the repo, checkout as native on client
* text=auto
